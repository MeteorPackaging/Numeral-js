This fork was a first attempt to publish `numeral:numeral` for Meteor.

The current wrapper package is inside the [numeraljs-core-wrapper](https://github.com/MeteorPackaging/numeraljs-core-wrapper) repository.
